// Preload script — runs in the renderer before the page loads.
// Kept minimal: the app communicates with the API server directly over HTTP.
// Add contextBridge.exposeInMainWorld() calls here if inter-process
// communication is needed in future.
